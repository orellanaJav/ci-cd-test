hola:
  description: hola
  environment: ${file(services/hola/setup/${opt:stage, 'dev'}/config.json):ENVIRONMENT}
  layers: ${file(services/hola/setup/${opt:stage, 'dev'}/config.json):LAYERS}
  handler: hola.lambda_handler
  module: services/hola/core
  name: test-stack-ci-hola
  tags:
    stack_name: test-stack-ci
    lambda_name: hola
  package:
    patterns:
      - '!./**'
      - 'services/hola/**'
      - '!services/hola/core/resources/*.yml'
      - '!services/hola/core/requirements.txt'
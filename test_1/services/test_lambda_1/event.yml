test_lambda_1:
  description: null
  environment: ${file(services/test_lambda_1/setup/${opt:stage, 'dev'}/config.json):ENVIRONMENT}
  handler: test_lambda_1.lambda_handler
  layers: ${file(services/test_lambda_1/setup/${opt:stage, 'dev'}/config.json):LAYERS}
  module: services/test_lambda_1/core
  name: test-1-test_lambda_1
  package:
    patterns:
    - '!./**'
    - services/test_lambda_1/**
    - '!services/test_lambda_1/core/resources/*.yml'
    - '!services/test_lambda_1/core/requirements.txt'
  tags:
    lambda_name: test_lambda_1
    stack_name: test-1
